# 2026-09-22 다른 AI 인수인계: 현재 변경사항과 개인 Render

## 1. 문서 목적

이 문서는 다음 작업자가 현재 상태를 다시 추측하지 않고 이어서 작업할 수 있도록 작성한 인수인계 문서다.

현재 핵심 목표는 다음과 같다.

```text
기존 팀원 Render
→ 로그인·사용자·보호자·Polar·알림·AI 등 팀원 기능 유지

새 개인 Render
→ OCR·약 등록·오늘 약·내 약·약 상세·DUR·복약 기록·달력 담당
```

비밀키의 실제 값은 이 문서에 기록하지 않는다.

---

## 2. Git 현재 상태

- 현재 브랜치: `woong2`
- 원격 브랜치: `origin/woong2`
- 현재 HEAD: `7c70425`
- `origin/main`과 비교한 현재 차이: `main`에만 17개, `woong2`에만 7개 커밋이 있음

최근 핵심 커밋:

```text
7c70425 fix: Render OCR SQLite 잠금 충돌을 방지한다
a6477a0 fix: OCR 및 함께먹기 약 이름 표시를 정리한다
834f60a fix: 약 목록의 중복 주성분 표기를 정리한다
76f4847 fix: OCR 확인 카드와 액제 단위를 구분한다
4fb015f fix: 홈 약 중복과 복용 횟수 표시를 정리한다
4054617 Merge remote-tracking branch 'origin/main' into woong2
789ebee fix: 충돌이 있을 때만 함께먹기 주의를 표시한다
```

`main`을 바로 배포하면 위의 최신 개인 기능 일부가 빠진다. 개인 기능 확인용 Render는 `woong2`를 사용한다.

현재 작업 폴더에는 Flutter가 만든 Linux 임시 링크 변경과 사용자가 별도로 만든 문서·TXT가 있다. 아래 파일은 현재 작업과 관계없이 임의 삭제·커밋하지 않는다.

```text
linux/flutter/ephemeral/.plugin_symlinks/*
진행계획서.md
OCR_기능_구조.txt
약자세히_기능_구조.txt
```

이번 대화에서 새로 만든 다음 파일은 아직 별도 커밋하지 않은 문서다.

```text
.env.render.example
0922_개인_Render_분리_구성_보관용.md
0922_다른AI_인수인계_현재변경사항.md
```

---

## 3. 현재 적용된 화면 기능

### 3.1 홈·목록·약 상세 이름

홈, 내 약 목록, 복약 관련 요약 화면과 약 상세 제목에서는 제품명 뒤에 중복으로 붙은 주성분 괄호를 숨긴다.

```text
변경 전: 아디팜정(히드록시진염산염)
변경 후: 아디팜정

변경 전: 코다론정(아미오다론염산염)
변경 후: 코다론정
```

주성분 값은 DB에서 삭제하지 않으며 약 상세와 OCR 카드의 `주성분` 항목에서 별도로 표시한다.

제형처럼 주성분이 아닌 괄호는 유지한다.

```text
제품정(서방정) → 제품정(서방정)
```

DB의 공식 제품명과 `official_product_name`은 전체 이름을 유지한다. 화면에 보여 주는 요약 이름만 축약한다.

### 3.2 OCR `이렇게 읽었어요`

OCR 확인 카드 제목에서도 주성분과 같은 괄호는 제거한다.

```text
아디팜정(히드록시진염산염)
→ 아디팜정
```

카드 아래의 다음 항목은 그대로 유지한다.

```text
주성분: 히드록시진염산염
```

액제 제품은 `알` 단위를 임의로 붙이지 않는다. OCR 카드의 복용량·횟수·일수 수정 동작도 유지한다.

### 3.3 약 함께먹기 주의

충돌 약 두 개를 보여 주는 흰색 카드에는 약 이름만 표시한다.

```text
아디팜정
       + 같이
코다론정
```

이름 아래에 있던 다음 종류의 짧은 설명은 제거했다.

```text
가려움이나 알레르기 증상을 줄…
심장 박동을 고르게 하려고 드시…
```

약 이름은 각 흰색 카드의 가로·세로 정중앙에 배치한다.

충돌 원인과 의료진 확인 안내는 아래 빨간 설명 상자에 그대로 유지한다. 실제 충돌이 없으면 함께먹기 경고 카드를 표시하지 않는다.

### 3.4 홈 약 표시

현재 브랜치에는 다음 동작도 포함되어 있다.

1. 홈에서 동일 약이 중복 표시되지 않도록 정리
2. 약 오른쪽에 `0.5알`, `1알` 대신 `하루 3회`, `하루 1회`처럼 복용 횟수 표시
3. OCR 등록 후 홈과 내 약 목록이 같은 등록 결과를 다시 조회

---

## 4. OCR 인식률 기준에 관한 현재 결론

현재 코드에는 `글자 인식률이 특정 값 이상이면 무조건 약으로 등록한다`는 단일 기준이 없다.

- CLOVA 글자 인식률: OCR 엔진이 제공하는 표시용 수치
- `70% 미만`: 화면에서 확인 필요로 보는 내부 기준
- 공식 약 확인률 `60% 미만`: 재촬영 권장 내부 기준
- 공식 약 확인률 `85% 이상`: 공식 약이 하나 이상 있으면 재촬영을 권장하지 않는 내부 기준

위 숫자는 식약처나 의료기관이 제시한 공인 기준이 아니라 앱 내부 운영 기준이다. 실제 약 반영의 핵심 조건은 OCR 이름이 식약처 공식 제품과 매칭되는지 여부다.

---

## 5. 새 개인 Render 상태

새 개인 Render 배포가 완료되었다.

- 서비스 URL: `https://alkong-yakong-j0jn.onrender.com`
- API 문서: `https://alkong-yakong-j0jn.onrender.com/docs`
- 상태 확인: `https://alkong-yakong-j0jn.onrender.com/health`
- 연결 브랜치: `woong2`
- 서비스 형태: Python 3 Web Service

확인된 `/health` 응답 상태:

```text
status: ok
version: 1.2.0
environment: production
database: sqlite:alkongyakong.db
demo_seed_enabled: false
ocr_engine: clova-ocr-v2
ocr_configured: true
```

즉, 서버 기동과 CLOVA OCR 환경변수 인식은 정상이다.

---

## 6. Render 생성 설정

```text
Root Directory: 빈칸
Build Command: pip install -r requirements.txt
Start Command: uvicorn app.main:app --host 0.0.0.0 --port $PORT
Health Check Path: /health
Pre-Deploy Command: 빈칸
Auto-Deploy: On Commit
Included Paths: 빈칸
Ignored Paths: 빈칸
Secret Files: 사용하지 않음
```

`app.main:app`을 Root Directory 또는 Build Command에 단독으로 넣으면 안 된다. Start Command의 `uvicorn` 뒤에만 사용한다.

---

## 7. 새 Render 환경변수

현재 코드가 사용하는 기본 환경변수는 다음과 같다.

```env
APP_ENV=production
DEMO_SEED_ENABLED=false

E_DRUG_API_KEY=<공공데이터포털 키>
DUR_API_KEY=<공공데이터포털 키>
MFDS_DRUG_PERMISSION_API_KEY=<공공데이터포털 키>

DUR_AUTO_SYNC=true
DUR_BOOTSTRAP_MAX_PAGES=20

CLOVA_OCR_ENABLED=true
CLOVA_OCR_API_URL=<CLOVA Invoke URL>
CLOVA_OCR_SECRET_KEY=<CLOVA Secret Key>
```

다음과 같은 일반 이름은 현재 코드가 읽지 않는다.

```text
database_url
api_key
```

`PORT`와 `RENDER`는 Render가 자동으로 제공하므로 직접 넣지 않는다. `LOCAL_API_BASE_URL`은 Render 환경변수가 아니라 Flutter 빌드 시 전달하는 값이다.

현재 개인 테스트에서는 Persistent Disk를 추가하지 않았다. 따라서 로컬 Windows DB 경로와 `/var/data` DB 경로를 Render 환경변수에 넣지 않는다.

---

## 8. 기존 팀원 Render에 유지할 기능

기존 팀원 Render와 기존 URL은 삭제하거나 덮어쓰지 않는다.

기존 서버에 유지할 기능:

1. 로그인·회원가입
2. 사용자 기본 정보와 계정 관리
3. 보호자 연결 및 보호자 화면
4. Polar 센서 연결
5. 심박수 측정·저장·조회
6. 푸시 알림 및 보호자 알림
7. AI 약사 챗봇
8. 팀원이 관리하는 기타 공통 기능

새 개인 Render 때문에 기존 `productionBaseUrl`을 변경하지 않는다.

---

## 9. 새 개인 Render가 담당할 기능

약 데이터가 두 DB로 갈라지지 않도록 아래 기능은 같은 새 Render를 사용한다.

1. 처방전 OCR
2. OCR 결과 확인 및 수정
3. 공식 약 검색과 식약처 제품 매칭
4. 처방전 등록 확정
5. 오늘 홈 약 조회
6. 내 약 목록 조회
7. 약 자세히 조회
8. DUR 함께먹기 분석 및 최근 결과 조회
9. `먹었어요` 복약 완료 저장·취소
10. 복약 기록 조회
11. 월별 복약 달력 조회
12. 처방 일정과 복용일 조회·수정
13. 손으로 약 입력

OCR만 새 Render로 보내고 홈·내 약 목록을 기존 Render에서 조회하면 안 된다.

```text
잘못된 연결
OCR 등록 → 개인 Render
홈 조회  → 기존 Render

결과
개인 Render DB에 저장된 약이 기존 Render 홈에는 보이지 않음
```

---

## 10. Flutter 앱 실행 방법

기본 약 기능 서버 주소를 개인 Render로 변경했으므로 일반 실행은 다음 명령이면 된다.

```powershell
flutter run
```

APK도 일반 명령으로 만들 수 있다.

```powershell
flutter build apk
```

이때 연결 구조는 다음과 같다.

```text
로그인·Polar·알림·팀원 공통 기능
→ 기존 팀원 Render

OCR·등록·오늘 약·내 약·상세·DUR·복약 기록·달력
→ 새 개인 Render
```

로컬 FastAPI를 시험해야 할 때만 주소를 명시해서 덮어쓴다.

```powershell
flutter run --dart-define=LOCAL_API_BASE_URL=http://로컬PC주소:8000
```

---

## 11. 컴퓨터를 껐다 켠 후

Render는 사용자 컴퓨터에서 실행되는 서버가 아니다. 컴퓨터를 껐다 켜도 Render 사이트에 들어가 서버를 수동으로 켤 필요가 없다.

다시 앱만 다음 명령으로 실행한다.

```powershell
flutter run
```

무료 Render가 휴면 상태라면 첫 요청에서 자동으로 깨어난다. 첫 요청만 수십 초 정도 지연될 수 있다.

---

## 12. SQLite 주의사항

현재 개인 Render는 Persistent Disk 없이 SQLite를 사용한다.

따라서 다음 상황에서 OCR로 새로 등록한 사용자 약 데이터가 초기화될 수 있다.

1. 새 커밋 자동 배포
2. Render 서비스 재시작
3. 무료 인스턴스의 파일시스템 초기화

이 문제는 서버 기동 실패가 아니라 무료 Render의 임시 파일 저장 특성이다. 현재는 개인 기능 테스트용이므로 우선 그대로 사용한다.

데이터 장기 보관이 필요해질 때만 Persistent Disk 또는 공용 DB 전환을 별도 진행한다. 지금 단계에서 임의로 DB 경로를 추가하지 않는다.

---

## 13. 확인 완료된 테스트

최근 변경 후 확인한 내용:

1. Flutter 정적 분석 이상 없음
2. OCR 이름 축약 공통 정책 테스트 통과
3. 복약 흐름 테스트 통과
4. 다양한 화면 크기·글자 배율의 화면 테스트 302개 통과
5. Render `/health` 200 OK
6. Render `ocr_configured: true`

---

## 14. 다음 작업자가 지켜야 할 사항

1. `woong2`의 OCR·약 상세·DUR·복약 기능을 단순히 `main` 버전으로 덮어쓰지 않는다.
2. 사용자가 명시하지 않은 로그인·Polar·알림 기능은 수정하지 않는다.
3. 약 등록과 약 조회가 서로 다른 서버로 갈라지지 않게 한다.
4. 비밀키 실제 값을 코드·Markdown·Git 커밋에 넣지 않는다.
5. Flutter 임시 링크와 사용자 작성 문서는 별도 요청 없이 커밋하지 않는다.
6. 개인 Render 변경 후에는 `OCR → 등록 → 홈 → 내 약 → 약 상세 → DUR → 복약 기록` 전체 흐름을 같은 사용자 ID로 확인한다.
7. 최종 배포 전에는 최신 `main`을 병합하고 로그인·Polar·알림 회귀 테스트까지 수행한다.
