# 알콩약콩 수정 계획 (2026-09-11 오후)

상태: **계획만** — 코드 수정은 지시 후  
근거: 에뮬레이터가 보는 배포 서버(`https://alkong-yakong.onrender.com`) JSON을 직접 받음

---

## 한 줄로

약은 맞게 들어와 있다. **이름을 성분으로 줄여 쓰고, 쉬운 분류·한 문장을 안 붙여서** 홈이 이상해 보인다. 배포 서버가 예전 형식으로 주고, 목록·상세 API는 배포에 없다.

---

## 지금 화면에 나오는 것 (확인함)

에뮬레이터 Android → 배포 서버 → 사용자 `mvp-user`.

아침 카드에 실제로 온 값:

- 아디팜: 허가명 `아디팜정(히드록시진염산염)` 인데, 카드 이름은 `히드록시진염산염 (알레르기·두통·어지러움)`
- 시메티딘: 허가명 `휴온스시메티딘정200밀리그램…` 인데, 카드 이름은 `시메티딘 (속쓰림·위·소화·알레르기)`
- 분류(`purpose_label`)·한 문장(`short_explanation`)은 **비어 있음**
- 용량 `0.50` 은 맞음
- 목 3종(아스피린 100mg 등)도 서버에는 있음. 앱이 걸러서 홈에서는 안 보임

그래서 홈에는 **히드록시진염산염 / 시메티딘 + 0.50** 만 남는다.

PC 로컬 DB의 체험 약(코다론·부루펜·게루삼)과는 **다른 데이터**다. 폰이 로컬을 안 본다.

---

## 나와야 하는 카드 (화면마다 같게)

한 약 = 네 칸.

1. **허가 이름** — 예: `아디팜정(히드록시진염산염)`
2. **짧은 분류** — 예: `가려움 완화 · 불안·긴장 완화` (증상 나열 `알레르기·두통·어지러움` 금지)
3. **한 문장** — 예: `가려움이나 불안·긴장을 줄이는 데 쓰이는 약이에요` (`처방받은 약이에요` 금지)
4. **투약량** — `0.50` / `1알` (제품 함량 200밀리그램은 이름에만)

---

## 화면별로 지금 상태

| 화면 | 지금 | 고쳐야 할 것 |
|------|------|----------------|
| 오늘 홈 | 성분명만, 분류·문장 없음 | 위 네 칸 |
| 복약 기록 | 같은 잘못된 이름 + 괄호 키워드 | 허가 이름만 |
| 복약 알림 | 홈과 같은 성분명 | 허가 이름 |
| 잠금 알림 | 성분명 + 괄호 키워드 | 허가 이름 + 용량 |
| 내 약 목록 | 배포에서 API 없음(404) → 불러오기 실패 | 목록 API 배포 + 같은 네 칸 |
| 약 자세히 | 상세 API도 404 | 상세 API 배포 + 같은 네 칸 |
| 함께먹기 | 이미 허가명 목록을 줌 | 이름만 홈과 같게 맞추면 됨 |
| 처방전 찍기 | 촬영 UI는 유지 | 이번 범위에서 화면 다시 그리지 않음 |

홈만 이상한 게 아니다. **기록·알림도 같은 잘못된 이름을 쓴다.** 목록·상세는 배포에 인터페이스가 없어 아예 실패한다.

---

## 원인 (수정 때 건드릴 곳)

1. **배포 `today-medicines`가 예전 형식**  
   이름 = 성분 + 괄호 키워드. 분류·문장 필드 없음.
2. **앱이 그 이름을 그대로 제목으로 씀**  
   괄호만 떼서 `히드록시진염산염`이 된다.
3. **배포에 `GET /users/{id}/medicines` 가 없음** (로컬 코드에는 있음).
4. **목 3종(MVP-ASP 등)이 배포 `mvp-user`에 아직 활성.**

로컬 백엔드 생성기는 이미 허가명·문장을 만들 수 있다. **그 코드가 배포 서버에서 안 돌고 있다.**

---

## 고치는 순서

### 0. 어디를 고칠지 정하기 (먼저)

데모를 **배포 서버**로 보여줄지, **PC 로컬 서버**로 보여줄지 하나만 고른다.

- 배포로 보면: 아래 1~3을 배포에 올린 뒤에야 폰 화면이 바뀐다.
- 로컬로 보면: 앱에 `API_BASE_URL`을 PC 주소로 넣고, 로컬 서버를 켠다. (로컬 `mvp-user`는 코다론·부루펜·게루삼이다. 히드록시진이 아님.)

정하기 전에는 앱만 고쳐도 에뮬레이터 화면은 안 바뀐다.

### 1. 서버 JSON을 카드 네 칸에 맞추기

`today-medicines` (그리고 목록·상세)가 이렇게 나가게 한다.

- `display_name` = 허가 제품명 (`아디팜정(히드록시진염산염)`)
- `purpose_label` = 짧은 분류
- `short_explanation` = `~약이에요` 한 문장
- `amount` = 투약량
- 이름에 `(알레르기·두통·어지러움)` 같은 키워드를 붙이지 않는다
- `처방받은 약이에요`는 보내지 않는다

로컬에 있는 `_medicine_item` / `load_medicine_guidance`를 **그대로 배포**하는 쪽이 이중 생성보다 안전하다.

### 2. 그 API를 배포에 올리기

- `GET /users/{id}/today-medicines` 새 형식
- `GET /users/{id}/medicines`
- `GET /users/{id}/medicines/{code}`

올린 뒤 curl로 아디팜·시메티딘 한 줄이 네 칸인지 확인한다.

### 3. 앱은 같은 네 칸만 그리기 (UI 리디자인 없음)

- 홈 · 목록 · 상세 · 기록 · 알림 · 잠금: **허가 이름** 사용 (`display_name` / `product_name`). 성분명을 제목으로 쓰지 않음
- 분류·문장이 있으면 홈·목록·상세에만 그림. 기록·알림은 이름(+용량)이면 됨
- 처방전 촬영 화면은 건드리지 않음

### 4. 배포 `mvp-user` 약 정리 (데이터)

- 목 코드 `MVP-ASP` / `MVP-AMLO` / `MVP-MET` 는 활성에서 빼거나 안 내려가게
- OCR로 넣은 아디팜·시메티딘은 **실약이니 유지** (이름·문장만 고침)
- 체험 시드로 코다론 세 종만 보여줄지는 별도 결정

### 5. 기기에서 확인

앱 **완전 종료 후** 다시 실행.

- 홈: `아디팜정(히드록시진염산염)` + 분류 + 한 문장 + `0.50`
- 목록·상세: 같은 이름·문장 (404 아님)
- 기록·알림: 같은 허가 이름
- 함께먹기: 홈과 같은 이름
- `아스피린 100mg` 없음
- `처방받은 약이에요` 없음

---

## 하지 않음

- 처방전 넣기 화면을 다시 그리지 않음
- 로그인 개편, Gemini를 상세 기본으로, DUR 외부 API 대규모 변경, 보호자 앱 전체
- 홈만 예외 처리하고 목록은 그대로 두는 것

---

## 완료 기준

- [ ] 홈 / 목록 / 상세 / 기록 / 알림이 같은 허가 이름을 본다
- [ ] 홈·목록·상세에 짧은 분류와 `~약이에요` 한 문장이 있다
- [ ] 투약량은 `0.50` / `1알`
- [ ] 목 3종이 어떤 화면에도 없다
- [ ] 목록·상세가 배포에서 404가 아니다 (배포로 데모할 경우)
