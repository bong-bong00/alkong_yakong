# 홈 아침 카드 둘째 줄 — 수정 계획

상태: **계획만**  
범위: 홈 아침(현재 슬롯) 카드 안 약 줄. 약 자세히·OCR·DUR·Render 배포 아님.  
이전 복구 계획(0922 병합 전 화면 등)은 이 작업에 쓰지 않는다.

---

## 화면 차이 (첨부)

**아침 카드** (`_DoseCard` 안 `_MedicineRow`)  
약 이름 + 오른쪽 `하루 N회`만 있다. 무슨 약인 줄이 없다.

**오늘 다른 약** (`_OtherDosesBlock`)  
같은 홈 카드 아래 회색 칸. 약 이름 다음 줄에 짧은 설명이 있다.

- 코다론정 → `심장 박동 약`
- 프리마란정 → `가려움 약`
- 아디팜정 → `가려움 완화 · 불안 긴장 완화`
- 휴온스시메티딘정200밀리그램 → `속쓰림 약`

이 둘째 줄은 주성분명(아미오다론, 메퀴타진)이 아니다. `Medicine.effect` → `cardPurposeLabel(purposeLabel)` 이다.

---

## 현코드

파일: `lib/features/dashboard/presentation/screens/patient_home_screen.dart`

- `_MedicineRow` (약 473행 근처): `displayName`만 그린다. 주석에 “무슨 약인지는 줄을 눌러서 보는 약 설명이 맡는다”고 적혀 있어 둘째 줄을 빼 둔 상태다.
- `_OtherDosesBlock` (약 572–583행): `displayName` 아래 `if (schedule.medicine.effect != null)` 로 그 짧은 분류를 그린다.

데이터는 이미 홈 JSON에 있다.

- `medication_controller.dart` `parse()`가 `purpose_label` → `Medicine.purposeLabel`
- `Medicine.effect` = `cardPurposeLabel(purposeLabel)` (`medication_models.dart`)
- 오늘 다른 약이 그 필드를 보여 주므로, 아침 줄만 안 그리는 것이다.

주성분 문자열은 `Medicine.ingredientLabel` (시메티딘 · 200밀리그램 등)이다. 첨부 2와 맞추려면 이걸 아침 둘째 줄에 넣지 않는다.

---

## 고칠 방향

아침 카드 한 장 안에서 약을 이렇게 쌓는다.

```text
코다론정
심장 박동 약                         하루 3회
프리마란정
가려움 약                            하루 3회
…
```

1. `_MedicineRow`의 이름 `Column`에 `_OtherDosesBlock`과 같은 둘째 줄을 넣는다.  
   `medicine.effect`가 있을 때만. 스타일은 다른 약 칸과 맞춤: `AppText.caption(size: 16.5)` (이름은 기존 `cardTitle` 21).
2. 오른쪽 `하루 N회` · 쉐브론 · `먹었어요` · `눌러서 설명 보기` · 오늘 다른 약 칸 레이아웃은 유지.
3. `effect`가 없으면 지금처럼 이름만. 빈 줄·가짜 분류를 만들지 않는다.
4. 약 이름 if 분기, 초록 “안전”, Gemini 문장 생성 없음. 표시 정책은 기존 `cardPurposeLabel`만 쓴다.
5. `_MedicineRow` 주석(“홈에 설명을 안 적는다”)은 이 변경에 맞게 고친다.

위젯만 고친다. `today-medicines` 파서·서버 필드 추가는 하지 않는다.

---

## Render를 갱신해야 하나

**아니요.** 앱이 그리는 줄이 빠진 것이고, 그 값은 이미 개인 Render 홈 JSON의 `purpose_label`이다. 첨부 2가 그 값으로 그려지고 있다. Flutter 다시 실행/재설치면 된다. `j0jn` 재배포는 이 화면에 필요 없다.
