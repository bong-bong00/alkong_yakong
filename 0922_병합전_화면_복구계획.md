# 0922 병합 전 화면 복구 계획

상태: **계획만** — 코드 수정은 지시 후  
기준: 깃허브 `woong2` 병합 직전 커밋 `1e808d1` (`https://github.com/bong-bong00/alkong_yakong/commit/1e808d1`)  
열어 보는 법: `git show 1e808d1:<경로>`  
목표 화면: 첨부 콜라주(홈 → 약 자세히 → 자세히 하단 함께먹기) + OCR 후 「이렇게 읽었어요」(이번 채팅 이미지)

약 이름 하드코딩 분기는 넣지 않는다. Gemini로 상세 문장을 만들지 않는다. 초록을 “안전” 의미로 쓰지 않는다. `AppColors.detailEmphasis`는 강조 글자에만 쓴다.

---

## 병합 전 깃허브에서 화면이 나오던 경로

아래는 `1e808d1` 코드 기준이다. 위젯 파일만 보면 안 된다. **이름·한 줄·주성분 설명·치료 줄은 서버 JSON → 표시 정책 → 화면** 순이다.

### A. 홈 (콜라주 왼쪽)

1. 앱이 `GET /api/v1/users/{userId}/today-medicines` 를 부른다.  
   - 앱: `lib/features/medication/application/medication_controller.dart` `MedicationController` / `parse()`  
   - 서버: `app/routes/dashboard.py` → `app/services/today_medication_service.py` `get_today_medicines()`
2. 약 한 줄 JSON을 `resolveMyMedicineCard()`로 고른다.  
   - `lib/features/medicines/domain/display_policy.dart`  
   - 제목: `cardOfficialName` (허가 제품명, 중복 주성분 괄호 제거) → `Medicine.ingredient` 필드에 **표시 이름**을 넣음 (`parse()` 113행 근처 `ingredient: card.name`)  
   - 한 줄: `cardSpokenOf(short_explanation) ?? cardSpokenOf(easy_category)`  
     `목적으로 처방`이 있으면 **버리고** `easy_category`를 씀. 아디팜 테스트가 이 규칙이다.
3. 아침 카드를 그린다.  
   - `lib/features/dashboard/presentation/screens/patient_home_screen.dart`  
   - `_DoseCard`: `dose.slot.spokenTime` (`아침 8시`) + `눌러서 설명 보기`  
   - `_MedicineRow`: `medicine.displayName` + `하루 $frequencyPerDay회` + `PillPhoto`  
   - `먹었어요` = `SeniorButton` `onTake`  
   - `오늘이 마지막이에요` = `lib/features/medication/domain/medication_models.dart` 잔여일 문구
4. 홈에 띄우는 부모.  
   - 일반: `lib/features/dashboard/presentation/screens/home_screen.dart` `onOpenDrug`  
   - 쉬운 모드: `lib/features/easy_flow/presentation/easy_flow_shell.dart` `onOpenDrug`

### B. 홈에서 약 눌러 → 약 자세히 (콜라주 가운데·오른쪽)

1. 줄 `InkWell.onTap` → `onOpenDrug(medicine)`.  
   - 일반 탭: `context.push('/medicines/$code')` (`home_screen.dart` 61행)  
   - 라우트: `lib/main.dart` `/medicines/:code` → `DrugDetailScreen`  
   - 쉬운 모드: `Navigator.push`로 같은 `DrugDetailScreen(medicineCode: code)`
2. `DrugDetailScreen._load()`가 `UserMedicinesController.loadDetail(code)` 호출.  
   - `lib/features/medicines/presentation/screens/drug_detail_screen.dart`  
   - `lib/features/medicines/application/user_medicines_controller.dart`  
   - `GET /api/v1/users/{userId}/medicines/{code}`
3. 서버 상세.  
   - `app/routes/users.py` → `app/services/user_medicines_service.py` `get_user_medicine()`  
   - 같은 응답 안에서 `reviewed_detail_payload()` (`app/services/drug_explain_service.py`)  
   - 프로필 생성: `app/services/medicine_detail_service.py` `_build_profile()` / `treatment_use_items()` / `_PURPOSE_GROUPS`  
   - 주성분 쉬운 설명: `app/services/medicine_detail_providers.py` `find_reviewed_ingredient_explanations()` + `ingredient_aliases`  
   - 앱이 `explanation.ingredient_explanation`, `treatment_uses`, `ingredient_highlight`를 `UserMedicine.fromJson`에 합침
4. `_DetailBody`가 칸을 나눈다 (`1e808d1` `drug_detail_screen.dart`).  
   - 상단 카드: `displayName`, `manufacturer`, `ingredientParts`+함량, `detailSpoken`, 칩 `easyPurposes`  
   - `주성분 설명`: `ingredientExplanation` + `_EmphasizedBodyText` (`ingredientHighlight`, `detailEmphasis`)  
   - `어떤 치료에 쓰이나요?`: **`treatmentUses` 우선** (제목 초록 + 쉬운 설명). 허가 번호 목록 아님  
   - `전체 허가 목적`: `extraOfficialUses`만 접힘  
   - `함께먹기 주의가 있어요`: `interactionStatus == 'risk_found'` 일 때만. 서버 DUR 요약을 그대로 그림

시메티딘 가운데 화면은 이 경로의 `treatment_use_items`가 `위산|속쓰림|역류`에 걸려 `속쓰림·위 불편감`을 만든 결과다.  
아디팜 오른쪽 하단은 같은 자세히 화면을 내린 것. `가려움|알레르기` + `불안|긴장` 두 줄, 그 아래 접힌 허가 목적, 그 아래 코다론 DUR 카드.

### C. OCR 인식 후 화면 (이번 이미지 「이렇게 읽었어요」)

이미지는 **처방전 사진을 읽은 뒤 확인 단계**다. 홈·자세히와 다른 위젯이다.

1. 촬영 확인 → `_read()`.  
   - `lib/features/prescription/presentation/screens/prescription_screen.dart`  
   - `POST /api/v1/prescriptions/ocr` (`timeout 90s`)  
   - 서버: `app/routes/prescription.py` → `app/services/prescription_service.py` `create_prescription_from_ocr()`  
   - OCR 파이프라인: `app/services/ocr/pipeline.py` `run_ocr_pipeline`  
   - 공식 약 매칭: `retrieve_official` + DB `medicines`  
   - 한 줄 설명: `sync_medicine_guidance()` (`app/services/pharmacist/easy_category.py`) → 미리보기 `short_explanation` / `easy_explanation` / `easy_category`  
   - 병합 전 주성분: `med_dict.get("ingredient")` 그대로 (`preferred_card_ingredient` 없음)
2. 성공하면 `_step = PrescriptionStep.confirm`.  
   - 같은 파일 `build` switch `case PrescriptionStep.confirm` → `_ConfirmScreen`
3. `_ConfirmScreenState.build`  
   - 헤더 `SeniorBackHeader(title: '이렇게 읽었어요')`  
   - 파란 테두리 안내: `약 ${_editedItems.length}가지를 찾았어요` / `틀린 곳이 있으면 눌러서 고쳐주세요.`  
   - 약마다 `_DrugCard` (`1e808d1` 약 1513행)  
   - 아래 `이대로 등록하기` / `다시 찍기`
4. 이미지에 보이는 카드가 고르는 값 (`1e808d1` `_ConfirmScreen` Builder).  

   | 화면 | 코드 | JSON |
   |---|---|---|
   | `프리마란정` / `아디팜정` | `_shortDrugName` → `compactProductName(drug_name, ingredient:)` | `drug_name` |
   | `주성분: 메퀴타진` | `_DrugCard`가 `compactIngredientSummary(ingredient)` + `ingredient_strength` | `ingredient_name` 또는 `ingredient` |
   | 한 줄 설명 | `_seniorExplanation` | `short_explanation` → 없으면 `easy_explanation` → `easy_category` |
   | `0.5정` / `3회` / `7일` | `_takeAmountLabel` / `_frequencyLabel` / `_durationLabel` | `dose_amount`+`dose_unit`, `frequency_per_day`, `duration_days` |
   | `복용 정보 고치기` | `_DrugCard` `SeniorButton` `onEditDosing` → `_editItem` | — |
   | 빨간 테두리 | `_needsCheck` (확인 필요·uncertain·`interaction_conflicts`) | — |

5. 이미지 아디팜 문장 `가려움 또는 불안·긴장을 완화할 목적으로 처방될 수 있어요.` 가 나온 이유.  
   - `_seniorExplanation`은 **원문을 그대로** 찍는다. `cardSpokenOf`를 안 탄다.  
   - 홈은 같은 문장을 `목적으로 처방`이라 버리고 `가려울 때 먹는 약이에요`를 쓴다 (`display_policy.dart` + `display_policy_test.dart`).  
   - 프리마란이 `가려움이나 알레르기 증상을 줄이는 데 쓰이는 약이에요.`인 것은 `easy_category.py` 쉬운 문장이 `short_explanation`에 그대로 온 경우다.  
   - 복구 때 확인 카드도 홈과 같게 `cardSpokenOf`를 타야 아디팜이 이미지처럼 잘린 `목적으로`로 안 남는다.

6. `1e808d1` `_DrugCard`에 `expanded` / `fieldConfidences` / `onFixName` 인자는 있었으나 **build는 그리지 않았다.** 이미지에 쉐브론·인식률이 없는 것이 병합 전 기본 카드다. 병합 후 `00846ea`가 메인 쪽 펼침 UI를 넣었다가, 로컬에서 다시 기본 카드로 줄인 상태다.

---


## 나와야 하는 화면 (첨부 그대로)

### 홈 (아침 슬롯)

- [ ] `오늘이 마지막이에요` (해당 약만)
- [ ] `아침 8시` / `눌러서 설명 보기`
- [ ] `코다론정` `하루 3회`
- [ ] `프리마란정` `하루 3회`
- [ ] `아디팜정` `하루 3회`
- [ ] `휴온스시메티딘정 200밀리그램` `하루 3회` (제품명. 성분명 제목 금지)
- [ ] `프레벨액0.25%` `하루 1회` (`1알` 금지)
- [ ] `먹었어요`

### 약 자세히 — 시메티딘 (첨부 가운데)

- [ ] 제목 `휴온스시메티딘정200밀리그램`
- [ ] `제조사: (주)휴온스`
- [ ] `주성분` `시메티딘 · 200밀리그램`
- [ ] 한 줄 `위산을 줄여 속쓰림과 위산 역류를 완화하는 약이에요.`
- [ ] 칩 `속쓰림 약`
- [ ] `주성분 설명` 본문  
      `시메티딘은 이 약의 주성분이에요. 위산과 관련된 속쓰림이나 위 불편감을 줄이는 데 사용해요.`  
      (`시메티딘은` 굵게, `위산과 관련된 속쓰림이나 위 불편감을 줄이는 데`는 `detailEmphasis`)
- [ ] `어떤 치료에 쓰이나요?`  
      `· 속쓰림·위 불편감` (`detailEmphasis`)  
      `위산과 관련된 속쓰림이나 위 불편감을 줄이는 데 사용해요.`
- [ ] `실제 처방 이유는 의료진에게 확인해 주세요.`
- [ ] 이 칸에 허가 원문 `1. 위·십이지장궤양…` 을 넣지 않는다

### 약 자세히 하단 — 아디팜 (첨부 오른쪽, 같은 자세히 화면)

- [ ] `어떤 치료에 쓰이나요?`
- [ ] `· 알레르기로 인한 가려움` + `알레르기로 인한 가려움 같은 증상을 줄이는 데 사용해요.`
- [ ] `· 불안·긴장` + `불안하거나 긴장된 증상을 완화할 목적으로 사용될 수 있어요.`
- [ ] `실제 처방 이유는 의료진에게 확인해 주세요.`
- [ ] `전체 허가 목적` 은 접힌 칸. 허가 번호 목록은 여기만
- [ ] 충돌이 있을 때만 빨간 카드 `함께먹기 주의가 있어요`
- [ ] 쌍 이름 `아디팜정 ↔ 코다론정`
- [ ] 본문 `두 약을 같이 드시면, 심장 박동을 만드는 전기 신호에 영향을 줘서, 박동이 느려지거나 불규칙해질 수 있어요.`
- [ ] `성분 위험요소: 심부정맥 위험 증가 가능`
- [ ] `약국이나 병원에 한 번 확인해 주세요.`
- [ ] 충돌 없으면 이 빨간 카드를 만들지 않는다

### 이렇게 읽었어요 (이전에 확정한 병합 전 카드)

- [ ] 헤더 `이렇게 읽었어요`
- [ ] `약 N가지를 찾았어요`
- [ ] `틀린 곳이 있으면 눌러서 고쳐주세요.`
- [ ] 제목은 허가 제품명. 예: `프레벨액0.25%` / `프리마란정` / `아디팜정`  
      괄호 성분명·영문 INN(`Prednicarbate`) 제목 금지
- [ ] `주성분:` 한글 + 함량. 예: `프레드니카르베이트 · 0.25%` / `메퀴타진` / `히드록시진염산염`  
      영문만 있으면 제품명 한글 괄호. 그것도 없으면 주성분 줄 숨김
- [ ] 한 줄은 홈과 같은 쉬운 문장. `목적으로 처방` 문장 금지  
      예: 아디팜 `가려울 때 먹는 약이에요` 또는 `가려움을 줄이는 데 쓰이는 약이에요.`
- [ ] `1회 투약량` / `1일 투여횟수` / `투약일수`
- [ ] 액제 1회 투약량은 `1회` (`1알` 금지)
- [ ] 카드마다 `복용 정보 고치기`
- [ ] `이대로 등록하기` / `다시 찍기`
- [ ] 쉐브론, `공식 약 확인됨`, `글자 인식률`, `자세히 확인하기`, `처방전과 같은 약이…` 없음
- [ ] `내일부터 이렇게 됩니다` / `N알` 미리보기는 이번 복구에 넣지 않음

---

## 지금 코드가 화면을 깨는 지점 (현재 워크트리 vs `1e808d1`)

아래는 추정 아님. `git show 1e808d1:<파일>` 과 지금 파일을 함수 단위로 맞춘 것이다.

### 시메티딘 자세히가 허가 원문 + 빈 주성분 카드가 되는 경로

1. `GET /api/v1/users/{id}/medicines/{code}` → `user_medicines_service.get_user_medicine()` → `drug_explain_service.reviewed_detail_payload()`.
2. 페이로드가 `treatment_uses`를 채울 때 **지금**은 같은 파일 54행 `def _treatment_use_items`를 부른다. 항목이 180자를 넘거나 `제목:본문` 형식이 아니면 `return []`. 시메티딘 효능 원문은 한 덩어리라 항상 빈 배열이다.
3. `_parse_official_purposes` (**지금** `medicine_detail_service.py` 514행): `all_items = [cleaned]` 한 줄. `representative = all_items if len(all_items) <= 3 else []` → 허가 원문 전체가 `approved_use_summary`가 된다.
4. Flutter `_DetailBody`: `treatmentUses.isEmpty`이면 else에서 `approvedUseSummary`를 `어떤 치료에 쓰이나요?`에 출력 → `1. 위·십이지장궤양…`.
5. 주성분 설명: `LocalReviewedIngredientProvider.find_reviewed`가 `ingredient_name.casefold() == ingredient["name"].casefold()`를 요구하고 `ingredient_aliases`를 안 본다. 조회 실패 → `ingredient_explanation` 빈 문자열.
6. `_compose_reviewed_ingredient_explanation` (**지금** 544행)에 단일 성분 `role_explanation + use_help` 조립이 없다. 조회가 돼도 `시메티딘은 이 약의 주성분으로, …`가 안 나온다.
7. `drug_detail_screen.dart` 173행: `!hasDetailContent || ingredientExplanation.isEmpty`이면 `주성분의 쉬운 설명을 아직 확인하지 못했어요` 카드를 그린다.

`1e808d1`이면 2번은 `from medicine_detail_service import treatment_use_items`이고, 효능에 `위산|속쓰림|역류|위궤양`이 있으면 `{title: 속쓰림·위 불편감, description: 위산과 관련된 속쓰림이나 위 불편감을 줄이는 데 사용해요.}`가 나온다. 5번은 별칭+`normalized_key`만 본다. 6번은 `_topic_particle`로 주성분 문장을 만든다.

### 아디팜 자세히 하단

같은 `treatment_use_items`가 비면 `_PURPOSE_GROUPS`의 `가려움|두드러기|알레르기` / `불안|긴장|초조|신경증` 두 줄이 안 나온다. 첨부 오른쪽 초록 두 줄은 그 튜플 그대로다.

### OCR 이미지(프리마란·아디팜)

- 위젯: `_ConfirmScreen` / `_DrugCard`. 크롬은 이미 병합 전 기본형.
- 한 줄: `_seniorExplanation` (803행)이 `short_explanation` 원문을 그대로 찍는다. `cardSpokenOf` 없음.
- 홈 `parse()` → `resolveMyMedicineCard` → `cardSpokenOf`가 `목적으로 처방`을 버린다.
- OCR 서버: `create_prescription_from_ocr`가 `guidance["short_explanation"]`를 미리보기에 넣는다. 아디팜 검토 문장이 `목적으로 처방될 수 있어요`면 확인 카드에 그대로 나온다.

---

## 고치는 방향 (코드, `1e808d1`에서 가져올 것)

원칙: 새로 발명하지 말고 `git show 1e808d1:<파일>`의 해당 함수를 되돌린다. 약 이름 if 분기는 넣지 않는다.

### 1. `app/services/medicine_detail_service.py`

- [ ] `_PURPOSE_GROUPS`를 `1e808d1` 49–95행 그대로. 형식 `(pattern, title, sentence)` 세 칸. 지금은 `(pattern, sentence)` 두 칸이라 제목이 없다.
- [ ] 첨부와 같은 세 줄은 이 튜플에서 나와야 한다.
  - `가려움|두드러기|알레르기|알러지` → 제목 `알레르기로 인한 가려움` / 설명 `알레르기로 인한 가려움 같은 증상을 줄이는 데 사용해요.`
  - `불안|긴장|초조|신경증` → `불안·긴장` / `불안하거나 긴장된 증상을 완화할 목적으로 사용될 수 있어요.`
  - `위산|속쓰림|역류|위궤양|십이지장궤양` → `속쓰림·위 불편감` / `위산과 관련된 속쓰림이나 위 불편감을 줄이는 데 사용해요.`
- [ ] `_topic_particle()` 복구 (`1e808d1`에 있고 지금 없음). 한글 받침 있으면 `은`, 없으면 `는`.
- [ ] `_parse_official_purposes`를 `1e808d1` 705–755행으로 교체. 표제 제거 → 줄/세미콜론/`_split_top_level_commas` 쪼개기 → `all`은 허가 조각, `representative`는 그룹 sentence 최대 3개.
- [ ] 지금 로직 삭제: `all_items = [cleaned]`, `representative = all_items if len<=3 else []`.
- [ ] `def treatment_use_items`를 `1e808d1` 758–793행으로 파일에 다시 선언. joined에 효능 조각을 모아 `_PURPOSE_GROUPS`를 돌며 `{title, description}` 최대 3개. 매칭 없으면 짧은 허가 항목 title만 (description 빈 문자열).
- [ ] `_compose_reviewed_ingredient_explanation`에 `1e808d1` 827–836행을 다시 넣는다.

```python
if len(ingredients) == 1:
    role = clean_ingredient_explanation(row.get("role_explanation"))
    use_help = clean_ingredient_explanation(row.get("use_help"))
    if role and use_help:
        name = ingredients[0]["name"]
        return f"{name}{_topic_particle(name)} 이 약의 주성분으로, {role} {use_help}"
```

- [ ] `_merge_profile_content` / `_profile_quality` (`1e808d1` `ensure_medicine_detail` 앞) 복구. 빈약한 재빌드가 검토 설명을 덮지 않게.
- [ ] `ensure_medicine_detail` 지금 146–161행: `source_hash` 불일치 때 `ai_explanation_cards`를 `OUTDATED`로 만드는 UPDATE를 뺀다. 해시 공식이 `official-v1:`로 바뀌면 시메티딘 검토 카드가 죽는다. 병합 전은 병합 유지.
- [ ] `_store_reviewed_ingredient`에 `is_displayable_ingredient_explanation` 가드 복구.
- [ ] `PARSER_VERSION`을 `3.0` 또는 `3.1`로 바꾸고 시메티딘·아디팜·코다론에 `ensure_medicine_detail`을 한 번 다시 탄다. `medicine_detail_profiles.approved_use_summary`에 허가 원문 한 덩어리가 남으면 앱이 그걸 다시 그린다.

### 2. `app/services/medicine_detail_providers.py` `find_reviewed`

- [ ] 지금 70–87행의 `ingredient_name` 완전 일치 + aliases 금지 삭제.
- [ ] `1e808d1` 64–94행으로 교체: `canonical_key = ingredient["key"]` → `ingredient_aliases.alias_key`로 canonical 조회 → `ingredient_explanations.normalized_key=? AND review_status='REVIEWED' AND source_verified=1` → displayable이면 `result[ingredient["key"]] = dict(row)`.
- [ ] SELECT 컬럼에 `role_explanation`, `use_help`를 반드시 포함 (`_compose`가 씀).
- [ ] `is_displayable_ingredient_explanation` 종결/길이 검사는 유지.

### 3. `app/services/drug_explain_service.py`

- [ ] import를 `1e808d1`처럼 `treatment_use_items`, `ingredient_entries`, `get_medicine_detail_profile`를 `medicine_detail_service`에서 가져온다.
- [ ] **지금 54행 `def _treatment_use_items` 전체 삭제.** 180자에서 빈 배열을 주는 이 함수가 자세히 본문을 허가 원문으로 떨어뜨린다.
- [ ] `reviewed_detail_payload`에서 `treatment_uses = treatment_use_items(approved_use_summary, approved_uses, all_approved_uses)`.
- [ ] `_ingredient_explanation_fallback` (`1e808d1` 54–95행) 복구. `ingredient_explanation`이 비고 status가 OUTDATED가 아닐 때 호출. 전 성분 reviewed 설명이 있으면 이어 붙이고, 없으면 `{이름}은(는) 이 약의 주성분이에요. 이 약은 {purpose}`. purpose에서 `공식 허가정보에서 확인한 대표 사용 목적이에요.`는 제외.
- [ ] `_ingredient_highlight`: 설명 문장 안에 있는 `use_help` 또는 `role_explanation`만 반환. 시메티딘 초록 구간이 이 값이다.
- [ ] `test/test_detail_phase1.py`는 지금 새 `_treatment_use_items`(추론 금지)를 검증한다. 복구하면 깨진다. 기대값을 `1e808d1` 키워드 그룹으로 바꾼다. 효능에 속쓰림이 있으면 title `속쓰림·위 불편감`, description `위산과 관련된 속쓰림이나 위 불편감을 줄이는 데 사용해요.`. 180자면 빈 배열 단언은 삭제.

### 4. `lib/features/medicines/domain/user_medicine_models.dart`

- [ ] `hasDetailContent`에서 `detailStatus`가 READY / OFFICIAL_ONLY / NEEDS_REVIEW일 때만 참인 가드를 뺀다 (`1e808d1` 187–192행). 필드가 있으면 참.
- [ ] `_treatmentUseList`: title이 비어 있지 않으면 살림. description 빈 문자열 허용. 지금 description 필수 요구는 키워드 폴백(title만)을 버린다.
- [ ] `TreatmentUse`는 description 기본값 빈 문자열 (`1e808d1`).

### 5. `lib/features/medicines/presentation/screens/drug_detail_screen.dart`

- [ ] `build` 로컬 변수를 `1e808d1` 101–106행 `extraOfficialUses`로. 지금 `allOfficialUses` 전체 나열을 접힌 칸에 넣지 않음.

```dart
final extraOfficialUses = medicine.allApprovedUses.where((purpose) {
  final normalized = purpose.trim();
  return normalized.isNotEmpty &&
      normalized != medicine.approvedUseSummary.trim() &&
      !medicine.approvedUses.any((shown) => shown.trim() == normalized);
}).toList();
```

- [ ] 상단 한 줄/칩의 `detailStatus != OUTDATED` 가드 제거 (`1e808d1`은 `detailSpoken != null`이면 그림).
- [ ] 173–185행 `아직 확인하지 못했어요` 카드 삭제. `1e808d1`에는 없고, 설명이 비면 주성분 설명 카드를 안 그릴 뿐이다.
- [ ] `어떤 치료에 쓰이나요?` 순서 유지: `treatmentUses`가 있으면 초록 제목+설명. else에만 summary/uses. 서버가 treatment_uses를 다시 채우면 허가 원문 else는 실행되지 않음.
- [ ] 함께먹기: `interactionStatus == 'risk_found'` 유지. 문구 하드코딩 금지. `interaction_summary` / `interaction_pair_label` / `interaction_risk_factor` 출력.

### 6. `lib/features/prescription/presentation/screens/prescription_screen.dart`

`_DrugCard` 레이아웃은 유지. 문장만 교체.

- [ ] `_seniorExplanation` (803–811행)을 아래로. `display_policy.dart`는 이미 import됨.

```dart
static String? _seniorExplanation(Map<String, dynamic> item) {
  final spoken = cardSpokenOf(item['short_explanation']?.toString()) ??
      cardSpokenOf(item['easy_explanation']?.toString()) ??
      cardSpokenOf(item['easy_category']?.toString());
  if (spoken == null || spoken == '처방받은 약이에요') return null;
  return spoken;
}
```

`목적으로 처방`이면 null → `easy_category`로 떨어짐. 아디팜 확인 카드에서 이미지의 잘린 `목적으로`가 사라져야 한다.
- [ ] `_DrugCard.explanation`에는 이 반환값만. JSON `short_explanation` 직접 출력 금지.
- [ ] 제목/주성분: 지금 `preferredCardIngredient` + `compactProductName` **유지** (프레벨 Prednicarbate 회귀 방지). `1e808d1`에는 없던 가드다.
- [ ] `_needsCheck` 빨간 테두리 유지.

### 7. `app/services/prescription_service.py` OCR 미리보기

- [ ] `ingredient = preferred_card_ingredient(...)` 유지.
- [ ] `official_spoken = guidance["short_explanation"]`는 약별 문장으로 다시 쓰지 않음. 앱 `cardSpokenOf`가 거른다.
- [ ] `_schedule_dates` / DUR `has_risk`는 이번 범위 밖.

### 8. 홈 제목 (콜라주 왼쪽)

홈 위젯은 다시 그리지 않음. `_MedicineRow`는 `medicine.displayName`만 그림.

- [ ] `GET /today-medicines`의 시메티딘 행에 `product_name=휴온스시메티딘정200밀리그램`이 있는지 확인. 비면 `cardOfficialName`이 성분으로 줄어든다.
- [ ] `today_medication_service` / `medicine_display.card_official_name`이 허가명을 내는지. 목 이름 `isMockDrugInfoName`이면 `parse()`가 이미 버린다.

### 9. 테스트가 가리키는 정책

- [ ] pytest: `treatment_use_items`에 속쓰림·위궤양 원문을 넣으면 첫 항목 제목 `속쓰림·위 불편감`. summary가 원문 전체가 아님.
- [ ] pytest: 효능에 가려움+불안 → 길이 2, 제목 `알레르기로 인한 가려움`, `불안·긴장`.
- [ ] pytest: 별칭 키로 `find_reviewed`가 `role_explanation`/`use_help`를 주고, 단일 성분 compose에 `주성분으로` 포함.
- [ ] `test/display_policy_test.dart`: 아디팜 `card.spoken`에 `목적으로 처방` 없음.
- [ ] Dart: `cardSpokenOf('가려움 또는 불안·긴장을 완화할 목적으로 처방될 수 있어요.')` isNull.
- [ ] `test/test_detail_phase1.py`의 “추론 카테고리 금지 / 180자면 빈 배열”은 병합 후 정책이다. 첨부 화면과 반대이므로 그 단언을 기준으로 남기지 않는다.

---

## 이번에 하지 않음

- [x] 확인 카드 쉐브론 / 글자 인식률 / 자세히 확인하기 다시 넣기 — 넣지 않음
- [x] `내일부터 이렇게 됩니다` `N알` — 넣지 않음
- [x] 투약일수 없을 때 종료일·일정 칸 병합 잔여 — 이번 화면 복구와 분리
- [x] DUR `has_risk` 미완료 처리를 병합 전 `false`로 되돌리기 — 하지 않음 (미완료를 안전으로 보이면 안 됨)
- [x] 약 이름 if 분기, 초록 “안전”, Gemini 상세 생성

---

## 완료 기준 (첨부 대조)

- [ ] 홈 아침 카드 다섯 약이 첨부 왼쪽과 같음
- [ ] 시메티딘 자세히가 첨부 가운데와 같음
- [ ] 아디팜 자세히 하단 치료 두 줄 + 함께먹기가 첨부 오른쪽과 같음
- [ ] 확인 화면에 영문 주성분·`목적으로 처방`·`1알` 액제가 없음
- [ ] 허가 원문은 `전체 허가 목적` 접힌 칸에만 있음
