# 홈 짧은 분류 `~약` 통일 — 수정 계획

상태: **계획만**  
범위: 홈 아침 줄 · 오늘 다른 약. 약 이름 하드코딩 없음.

---

## 규칙

홈 둘째 줄은 **주제를 모두 적고**, 구분은 **쉼표**, **`약`은 맨 끝에 한 번**.

- `외 1가지` / `외 n개` 는 쓰지 않는다. 빠진 기능이 있어 보인다.
- 기능마다 `약`을 붙이지 않는다. (`가려움 약, 불안 긴장 약` 금지)
- 단어 사이 가운데점 `·` `ㆍ` 는 공백. (`불안·긴장` → `불안 긴장`)
- 끝의 `완화` / `약` / `제` 는 주제에서 뗀 뒤, 마지막에 `약` 한 번만 붙인다.

| 기능 수 | 홈 |
|---|---|
| 1 | `가려움 약` / `심장 박동 약` |
| 2 | `가려움, 불안 긴장 약` |
| 3+ | `가려움, 불안 긴장, 속쓰림 약` (전부, 쉼표) |

구현: `display_policy.dart` 홈용 함수 → `Medicine.effect`. 아침 줄과 오늘 다른 약 공통.

레이아웃: `하루 N회`는 이름과 한 줄, 분류는 그 아래.

테스트: 위 세 형식. `외` 없음. 제품명/코드 단언 없음.

---

## Render

**아니요.**
