# 알콩약콩 진행계획서

작성: 2026-09-11  
형식: 완료 시 `[ ]` → `[x]`  
관련: `수정계획.md`, `백엔드_재검수_화면연결_계획.md`, `사용자_흐름.txt`

---

## 전체 목표

- [x] 홈 · 내 약 목록 · 처방전 확인 · 상세 · 함께먹기가 **같은 내 약** + **같은 쉬운말**을 본다
- [x] `DrugInfo` 목(메트포르민 500mg 등)이 **어떤 화면에도** 나오지 않는다
- [x] 투약량은 `0.50` / `1알`. 제품 함량(200밀리그램)은 이름에만 남긴다

---

## Phase 0 — 사전 점검 (재검수)

- [x] `today-medicines` 활성 약·허가명·쉬운말 필드 확인
- [x] 내 약 목록 전용 API 없음 확인
- [x] `drug-explain/{code}`와 홈 쉬운말 문장 불일치 확인
- [x] `medicine_key_cautions` 1건(아디팜) 확인
- [x] Flutter `DrugInfo` / 라우트 미연결 확인
- [ ] 실제 로그인 `user_id`로 QA용 응답 JSON 스냅샷 저장 (today / ocr / confirm / dur)

---

## Phase 1 — 백엔드: 내 약 · 상세 API

### 1-1. 목록 API

- [x] `GET /api/v1/users/{user_id}/medicines` 라우트 추가
- [x] 활성 `user_medicines`만, **약 종류당 1행** (시간대 중복 없음)
- [x] 응답 필드: `medicine_code`, `display_name`, `amount`, `purpose_label`, `short_explanation`, `key_caution`
- [x] 복용 필드: `dosage`, `frequency_per_day`, `administration_times`
- [x] `today_medication_service._medicine_item` / `load_medicine_guidance` 재사용 (생성기 이중화 금지)
- [x] 단위 테스트 또는 API 스모크 테스트

### 1-2. 상세 API

- [x] `GET /api/v1/users/{user_id}/medicines/{medicine_code}` 라우트 추가
- [x] 목록 1건 + `easy_purposes[]`, `key_cautions[]`, `purpose_notice`
- [x] 본인 약이 아니면 404

### 1-3. today-medicines

- [x] 스케줄 doses 동작 유지 (회귀 확인)
- [x] API 역할 문서화: 목록=`/medicines`, 오늘 차=`/today-medicines`

**Phase 1 완료 기준**

- [x] curl/Postman으로 목록·상세 JSON이 홈 카드와 이름·문장 일치

---

## Phase 2 — 쉬운말 · 주의 데이터

### 2-1. 주의 문장

- [x] `derive_key_cautions_from_medicine` 규칙 확대 (졸음·운전, 임신, 간·신장, 알코올 등)
- [x] `sync_medicine_guidance`로 기존 약 backfill
- [x] 아디팜 외 3종 이상 `key_caution` non-null 확인

### 2-2. easy_category_map

- [x] 매칭 실패 → `처방받은 약이에요` 비율 샘플링 (10~20개)
- [x] 누락 키워드·spoken_map만 사전 추가

### 2-3. drug-explain 정책

- [x] 상세·목록 = purposes API만 사용 (Gemini 카드는 챗 전용) — **정책 확정**
- [x] (선택) drug-explain `easy_summary`를 `short_explanation`과 통일 — **미선택 시 생략**

**Phase 2 완료 기준**

- [x] 홈 · OCR 확인 · 목록 API에서 같은 생성기로 문장 생성

---

## Phase 3 — Flutter: 화면 연결

### 3-1. 라우트

- [x] `main.dart`: `/my-medicines` 추가
- [x] `main.dart`: `/medicines/:code` (또는 query) 추가
- [x] `mypage_screen.dart`: 「내 약 목록」→ `/my-medicines` (`_todo` 제거)

### 3-2. 내 약 목록

- [x] `MyMedicinesScreen` → `GET .../medicines` 연동
- [x] `DrugInfo.all` 폴백 제거
- [x] `DrugInfo.find` 제거
- [x] 빈 목록 UI + 처방전 찍기 CTA
- [x] 한 줄: 이름 / 짧은 분류 / 한 문장 / 용량 (홈과 동일)

### 3-3. 약 상세

- [x] `DrugDetailScreen` → 상세 API 또는 목록 전달 데이터
- [x] `DrugInfo` 의존 제거
- [x] 섹션: 이름 → 분류 → 한 문장 → 주의 → 복용
- [x] 허가 원문 긴 효능 미표시

### 3-4. 오늘 홈

- [x] `PatientHomeScreen`: 약 이름 탭 → 상세 화면

### 3-5. 데모 정리

- [x] `medication_controller._demoToday` 서버 시드와 문장 일치 (또는 네트워크 실패 시만)
- [x] `DrugInfo.dart` 목록·상세 미사용 처리 (deprecate 또는 삭제)

**Phase 3 완료 기준**

- [x] 홈 · 목록 · 상세에서 같은 약의 이름·문장·용량 일치
- [x] `아스피린 100mg` 문자열 없음

---

## Phase 4 — 처방전 · OCR · 손입력

### 4-1. 손입력

- [x] `prescription_screen`: 「손으로 적기」→ `ManualMedicineScreen` / `AddMedicineScreen`
- [x] 저장은 `POST prescriptions/confirm` 재사용
- [x] 공식 미매칭 시 등록 불가 + 안내

### 4-2. OCR 용량

- [x] 시메티딘 `0.50` vs `200밀리그램` 회귀 테스트 통과
- [x] `test_ocr_drug_filter.py` 유지·추가

### 4-3. 필드명 정리

- [x] OCR 확인 카드: `easy_explanation` / `short_explanation` / `easy_category` 앱·API 통일

**Phase 4 완료 기준**

- [x] 촬영 → 확인 → 등록 → 홈·목록 동일 약·문장
- [x] 손입력도 같은 confirm 경로

---

## Phase 5 — 함께먹기 · 챗봇

### 5-1. DUR 완성도

- [ ] (요구 시) 체험 시드: 코다론 + 아디팜 병용 데모
- [x] `mvp-user` 또는 가입 플로우에 `birth_date` → 연령 검사 COMPLETE

### 5-2. DUR 결과 재사용

- [x] 챗봇 「같이 먹으면」: `load_latest_dur_context` 우선
- [x] 약 목록 변경 시만 `analyze_dur` 재호출
- [x] `chat_pipeline._together_from_dur` 무조건 재분석 제거 검토

### 5-3. DUR 화면

- [x] 하드코딩 `김복자` / `딸 지안` → users / guardians API

**Phase 5 완료 기준**

- [x] OCR 등록 직후 DUR과 챗봇 「같이 먹으면」이 같은 충돌 설명

---

## Phase 6 — 정리 · 테스트 · 문서

### 6-1. 화면 통일

- [x] 챗봇 공식 화면 1개로 (`DrugExplainScreen` vs `PharmacistChatScreen` 결정)
- [x] `EasyFlowShell` 미사용 시 경로 정리

### 6-2. 테스트

- [x] `GET /medicines` 목록·상세 테스트
- [x] 홈·OCR·목록 JSON 필드 동일성 테스트
- [x] DUR latest 재사용 테스트

### 6-3. 문서

- [x] `사용자_흐름.txt` 내 약 목록·상세 문단 갱신
- [x] `수정계획.md` / 이 파일 완료 항목 반영

**Phase 6 완료 기준**

- [x] 전체 완료 기준(아래) 전부 체크

---

## Phase 7 — 메인 홈 복구 후 데이터 회귀 (이번 작업)

메인 UI는 `main` 화면을 다시 가져왔으나, 홈 카드에 **DrugInfo 목 3종**과 **효능 키워드 나열**이 다시 보였다.

스크린샷 기준 잘못된 줄:

- `아스피린 100mg` / 피 묽게, `암로디핀 5mg` / 혈압 낮춤, `메트포르민 500mg` / 혈당 조절
- `히드록시진염산염` / 알레르기·두통·어지러움, `시메티딘` / 속쓰림·위·소화·알레르기

### 7-1. 표시 계약 (홈 · 목록 · 상세 동일)

- [x] 한 줄: **허가 이름** + **짧은 분류**(혈압약·피가 굳지 않게 하는 약) + **한 문장(`~약이에요`)** + **투약량**. `처방받은 약이에요`는 카드에 쓰지 않는다
- [x] `피 묽게` → `피가 굳지 않게 하는 약` (`easy_category_map` + `medicine_purposes`)
- [x] `알레르기·두통·어지러움` 같이 점(·)으로 이은 증상 나열은 분류로 쓰지 않는다
- [x] `아디팜`처럼 검토된 `가려움 완화 · 불안·긴장 완화`는 유지한다
- [x] 처방전으로 넣은 실약(히드록시진·시메티딘 등)은 숨기지 않는다

### 7-2. 목(DrugInfo) 차단

- [x] 이름 `아스피린 100mg` / `암로디핀 5mg` / `메트포르민 500mg`은 홈·목록·today API에서 제외
- [x] 앱·백엔드가 **같은 이름 규칙**을 쓴다 (한쪽만 거르지 않음)
- [x] 알림 설정 화면에 목 약 이름을 하드코딩하지 않는다 (오늘 약 목록을 쓴다)
- [x] AI 약사 추천 답에서 메트포르민·암로디핀 목을 빼 일반 안내로 바꾼다

### 7-3. 생성기

- [x] `purpose_label`은 `완화`·`약`·`제` 분류만 이어서 붙인다 (약사 REVIEWED는 유지)
- [x] 키워드만 쌓인 저장 행이 있어도 카드 분류는 비우고 한 문장만 쓴다

### 7-4. 확인 (기기)

- [ ] 앱은 **완전 종료 후** 다시 실행해야 한다 (핫 리로드만으로는 이전 목록이 남을 수 있음)
- [ ] Android 실기기는 기본이 배포 서버. 로컬 시드(코다론·부루펜·게루삼)를 보려면 `API_BASE_URL`을 로컬에 맞춘다

**Phase 7 완료 기준**

- [x] 홈에 목 3종이 없다 (앱·API 필터 + 테스트)
- [x] 홈에 증상 키워드 나열이 없다 (생성기 + 화면)
- [x] 실약이면 이름·한 문장·투약량(시메티딘 `0.50`)이 보인다

---

## Phase 8 — 배포 JSON · 화면 이름 통일 (다음 작업)

배포 서버를 직접 조회한 결과. 앱 필터만으로는 홈이 의도와 같아지지 않는다.

- [ ] 데모 대상 서버 확정 (배포 올리기 / 로컬 `API_BASE_URL`)
- [ ] `today-medicines` `display_name` = 허가 제품명. 괄호 키워드 없음
- [ ] `purpose_label` · `short_explanation` 채움. `처방받은 약이에요` 없음
- [ ] 배포에 `GET /medicines`, `GET /medicines/{code}` 올리기 (지금 404)
- [ ] 홈·목록·상세·기록·알림이 같은 허가 이름을 씀
- [ ] 배포 `mvp-user`의 목 3종(MVP-ASP/AMLO/MET) 활성 정리
- [ ] 기기에서 아디팜·시메티딘 네 칸 확인

**Phase 8 완료 기준**

- [ ] 홈에 `아디팜정(히드록시진염산염)` + 분류 + 한 문장 + `0.50`
- [ ] 목록·상세 404 아님 (배포 데모 시)
- [ ] 기록·알림도 같은 허가 이름

---

## 이번 범위 밖 (별도 지시)

- [ ] 로그인·회원가입 전면 개편
- [ ] drug-explain Gemini 카드를 상세 기본으로 (정책 B 미선택 시)
- [ ] DUR 외부 API 대규모 변경
- [ ] 보호자 앱 전체 연동
- [ ] UI 전면 리디자인

---

## 진행 로그

| 날짜 | Phase | 내용 |
|------|-------|------|
| 2026-09-11 | 0 | 백엔드 재검수 완료, 수정계획·진행계획서 작성 |
| 2026-09-11 | 1·3 | 내 약 목록·상세 API, Flutter 라우트·화면 연결 |
| 2026-09-11 | 2·4·5·6 | 주의 문장 확대, 손입력, DUR 재사용, 문서·테스트. 범위 밖 항목은 미착수 |
| 2026-09-11 | 7 | 메인 UI 복구 후 홈에 목·키워드 나열 재발. 표시 계약·목 차단·생성기 수정 |
| 2026-09-11 | 8 | 배포 JSON이 예전 형식·목록 API 404. 수정계획.md에 다음 작업 정리 (미착수) |
