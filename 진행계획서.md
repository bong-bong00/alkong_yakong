# 알콩약콩 날짜별 진행계획서

작성 기준일: 2026-09-13  
표시 규칙: 완료한 코드 작업은 `[x]`, 앞으로 할 코드 작업과 사용자 확인 항목은 `[ ]`

> 이 문서는 작업을 Phase가 아니라 실제 작업 날짜별로 관리한다.  
> 기기·화면 실테스트는 사용자가 직접 진행한다. 개발 작업에서는 코드 수정 후 아래에 지정한 자체 자동 테스트 1개만 수행하고, 나머지는 사용자 테스트 목록으로 전달한다.

---

## 2026-09-11 — 완료한 작업

### 내 약·오늘 약 API

- [x] `GET /api/v1/users/{user_id}/medicines` 목록 API 추가
- [x] 활성 사용자 약만 약 종류별 한 행로 반환하도록 정리
- [x] 오늘 홈과 내 약 목록이 같은 약 정보 변환 함수를 사용하도록 통일
- [x] 제품명, 주성분, 복용량, 복용 횟수, 복용 시간대를 분리
- [x] 화면용 제품명에서 수출명 괄호를 제거하는 정리 규칙 추가
- [x] 단위가 없는 복용량을 무조건 `알`로 표시하지 않도록 방어 처리

### 홈·내 약 목록·기존 상세 화면

- [x] 홈 약 행에서 제품명 아래에 `주성분:` 줄을 별도로 표시
- [x] 홈에는 짧고 자연스러운 설명 한 문장만 표시
- [x] 목적 이름과 설명이 같은 뜻을 반복하지 않도록 정리
- [x] 긴 목적 목록은 홈에서 축약하고 상세 화면에서 모두 확인하도록 구성
- [x] 일반 화면과 쉬운 화면에서 약 행을 눌러 상세 화면으로 이동하도록 연결
- [x] 상세 화면의 Material 위젯 누락 오류 수정
- [x] 내 약 목록을 실제 사용자 약 API에 연결
- [x] 내 약 목록에서도 약을 눌러 동일한 상세 화면으로 이동하도록 연결

### OCR·복용정보

- [x] OCR 결과의 숫자 퍼센트를 공식 약 후보 매칭 신뢰도로 표시
- [x] 신뢰도를 제공하지 않는 Gemini 결과에는 임의 퍼센트를 표시하지 않도록 구분
- [x] OCR 확인 화면에 제품명과 주성분을 분리해서 표시
- [x] 공식 약 설명과 사용자별 복용정보를 분리해서 표시
- [x] OCR 등록 시 복용량·단위·하루 횟수·복용 일수·복용 시간대 저장 경로 보강
- [x] OCR 결과에 복용정보가 없을 때 확인 필요 안내 표시
- [x] OCR 등록 약이 오늘 홈과 내 약 목록에 반영되도록 연결

### 함께먹기·DUR

- [x] 등록된 사용자 약 기준으로 함께먹기 검사 연결
- [x] 아디팜정과 충돌을 확인할 수 있는 비교 약 데이터 준비
- [x] 충돌 결과에서 대상 약과 주의 이유를 확인할 수 있도록 응답 구조 정리

### 약 상세 설명 1차 기반

- [x] 기존 상세 API의 실제 응답과 빈 설명 발생 조건 확인
- [x] 코다론정을 예시로 제품명·주성분·상세 설명·주의·복용법을 나누는 구조 적용
- [x] 상세 설명을 한 줄 요약과 구분해 내려주는 응답 필드 추가
- [x] 화면에서 간단 설명 대신 상세 설명을 우선 표시하도록 연결
- [x] 검토된 설명이 없을 때 임의 내용을 만들지 않고 준비 중 상태를 표시

---

## 2026-09-13 — 오늘 진행할 작업

### 목표

코다론정에만 개별 적용된 상세 설명을 공통 시스템으로 바꾼다. 현재 앱 DB의 모든 약과 앞으로 OCR에서 공식 의약품 코드로 매칭되는 약 모두가 같은 절차로 상세 설명을 준비하고 표시하도록 한다.

상세 정보의 출처는 다음 세 층으로 분리한다.

1. **성분 공통 정보**: 같은 성분을 가진 여러 제품이 함께 재사용하는 쉬운 설명
2. **제품 공식 정보**: 해당 제품의 허가 효능·효과, 용법·용량, 사용상 주의사항
3. **사용자 복용 정보**: OCR 처방전에서 확인한 1회량, 하루 횟수, 복용 일수와 시간대

### 1. 상세 설명 저장 구조 일반화

- [x] 성분별 검토 설명을 저장하는 `ingredient_explanations` 구조 추가
- [x] 띄어쓰기·염 형태·복합제 표기를 정리한 성분 정규화 키 추가
- [x] 약 제품별 상세 결과를 저장하는 `medicine_detail_profiles` 구조 추가
- [x] 상세 준비 작업 상태를 관리하는 `medicine_detail_jobs` 구조 추가
- [x] 작업 상태를 `PENDING`, `FETCHING`, `DRAFT`, `READY`, `FAILED`, `OUTDATED`로 구분
- [x] 공식 원문 변경 여부를 판단할 `source_hash`와 설명 버전 저장
- [x] AI가 만든 설명은 확정 데이터와 분리해 초안·이력으로 저장
- [x] 기존 코다론정 전용 상세 데이터를 공통 상세 구조로 이전

### 2. 공통 상세 준비 서비스 구현

- [x] 모든 진입점이 호출할 `ensure_medicine_detail(medicine_code)` 공통 함수 추가
- [x] 제품 코드를 상세 정보의 기준 키로 사용
- [x] 먼저 로컬 식약처 의약품 DB에서 공식 정보를 조회
- [x] 로컬에 부족한 항목만 백그라운드에서 공식 외부 API로 보완
- [x] 상세 화면 요청 중 외부 API나 Gemini를 직접 기다리지 않도록 처리
- [x] 단일 성분과 복합 성분을 나누고 각 성분 설명을 조합
- [x] 허가 효능·효과를 사용자용 `이 약은 왜 쓰나요` 항목으로 정리
- [x] 제품 공식 용법과 사용자의 실제 처방 복용법을 섞지 않도록 분리
- [x] 주의사항을 `꼭 기억할 점`과 `바로 도움을 받아야 하는 경우`로 분리
- [x] 문장이 잘리거나 의미가 불완전한 부작용 정보는 표시하지 않도록 검증
- [x] 같은 성분 설명을 제품마다 다시 생성하지 않고 재사용

### 3. 설명 생성과 검토 정책

- [x] 공식 자료만으로 안전하게 만들 수 있는 기본 상세는 `OFFICIAL_ONLY`로 제공
- [x] 성분 작용 설명은 검토 완료된 내용만 사용자에게 표시
- [x] Gemini 초안 상태 `DRAFT`는 사용자 화면에서 숨김
- [x] `공식 자료 확인 여부`와 `사람 검토 여부`를 서로 다른 값으로 관리
- [x] `누가 생성했는지`와 `현재 어느 데이터에서 제공하는지`를 분리해서 기록
- [x] 실제 처방 이유를 단정하거나 약 변경·중단을 지시하는 문장 방지

### 4. 현재 DB 전체 약 상세 준비

현재 점검 기준으로 앱 약 테이블에는 약 60개, 사용자 약 코드는 11개가 있으며 식약처 로컬 데이터는 약 42,993건이다. 기존 상세 동기화가 끝난 항목은 일부이므로 일괄 준비 작업이 필요하다.

- [x] 앱 약 테이블의 전체 약을 상세 준비 작업 큐에 등록
- [x] 현재 사용자의 활성 약 11개를 우선 처리
- [x] 공식 허가 정보가 충분한 약은 `OFFICIAL_ONLY` 또는 `READY`로 저장
- [x] 검토된 성분 설명이 있는 약은 즉시 상세 화면에 연결
- [x] 상세 준비 작업을 다시 실행해도 중복 레코드가 생기지 않도록 처리
- [x] 원본 공식 정보가 바뀐 약만 `OUTDATED`로 전환해 재처리

### 5. OCR 신규 약 자동 연결

- [x] OCR 후보가 공식 의약품 코드로 확정된 직후 상세 준비 작업 자동 등록
- [x] 처방전 최종 등록 시 상세 프로필 존재 여부 재확인
- [x] 같은 약을 여러 번 OCR해도 상세 작업은 한 번만 생성
- [x] 상세 준비 실패가 처방전 등록 자체를 막지 않도록 저장 구간을 분리하고 실패를 격리
- [x] 등록 직후에는 제품명·주성분·복용정보·공식 기본 정보를 즉시 표시
- [x] 백그라운드 상세 준비 완료 후 별도 앱 업데이트 없이 다음 조회에서 반영
- [x] 공식 코드로 매칭되지 않은 OCR 원문에는 임의 상세 설명을 생성하지 않음

### 6. 상세 API 응답 통일

- [x] 상세 상태를 `READY`, `OFFICIAL_ONLY`, `PENDING`, `FAILED`, `OUTDATED`로 반환
- [x] `medicine`, `explanation`, `patient_dosage`, `official_usage`, `safety`, `source` 영역 분리
- [x] 복합제는 모든 주성분과 각 성분 역할을 함께 반환
- [x] 자료 출처와 검토 상태를 응답에 포함
- [x] 상세 준비 중이어도 제품명·주성분·사용 목적·복용법 기본 정보는 반환
- [x] 홈·내 약 목록 응답에는 긴 상세 문장을 싣지 않음
- [x] 기존 앱 버전이 깨지지 않도록 현재 필드를 일정 기간 함께 유지

### 7. Flutter 상세 화면 상태 처리

- [x] `READY`: 검토된 상세 설명과 전체 주의사항 표시
- [x] `OFFICIAL_ONLY`: 공식 정보 기반 설명과 출처 안내 표시
- [x] `PENDING`: 기본 정보와 `자세한 설명을 준비하고 있어요` 표시
- [x] `FAILED`: 기본 정보 유지, 다시 시도할 수 있는 안내 표시
- [x] `OUTDATED`: 기존 안전 정보 유지, 최신 정보 갱신 중 안내 표시
- [x] 성분이 여러 개면 성분명과 역할을 읽기 쉬운 묶음으로 표시
- [x] 실제 처방 복용법과 제품 공식 용법을 시각적으로 분리
- [x] 일반 모드와 쉬운 모드가 같은 사실을 서로 다른 난이도의 제목과 안내로 표시

### 오늘 코드 작업 완료 기준

- [x] 코다론정 전용 분기 없이 공통 상세 준비 서비스가 동작하는 코드 완성
- [x] 기존 DB 약 전체를 대상으로 상세 준비 작업을 실행할 수 있는 코드 완성
- [x] OCR 신규 공식 약이 자동으로 상세 준비 대상이 되는 코드 완성
- [x] 상세 준비 상태별 API 응답과 화면 분기 코드 완성
- [x] 배포·기기 실테스트 없이 사용자가 확인할 테스트 목록 정리

### 자체 테스트 — 1개만 수행

- [x] **공통 상세 흐름 통합 자동 테스트 1개**: 기존 DB 약 1개와 OCR로 신규 등록한 공식 코드 약 1개를 같은 테스트 안에서 처리해, 상세 준비 작업 생성 → 상세 API 기본 응답 → 준비 완료 후 상세 설명 반영까지 확인한다. 외부 API·Gemini는 가짜 응답으로 대체하며 실제 기기, 화면 실행, 배포 서버 테스트는 하지 않는다. (`1 passed`)

---

## 2026-09-13 — 상세 설명 품질 2차 수정 계획

### 확인된 문제

코다론정처럼 사람이 검토한 `READY` 설명은 자연스럽지만, 나머지 `OFFICIAL_ONLY` 약은 공식 효능 원문을 단순 분리해서 다음 문제가 공통으로 발생한다.

- [x] `이 약의 주성분은 ○○이에요` 수준에서 끝나고 실제 성분 역할을 설명하지 못함
- [x] `다음 질환에도 사용할 수 있다` 같은 문서 제목과 안내 문장이 반복됨
- [x] 첫 요약 문장이 아래 목적 목록에 다시 표시됨
- [x] 질환명과 전문용어가 우선순위 없이 너무 많이 나열됨
- [x] 한 문장이 지나치게 길거나 중간에서 잘린 상태로 표시될 수 있음
- [x] 공식 허가 범위와 사용자가 실제로 처방받은 이유의 구분이 약함

### 목표 화면 구조

상세 화면은 모든 약에서 다음 순서로 통일한다.

1. **약 이름·주성분·홈과 같은 한 줄 설명**
2. **주성분 설명**: 성분이 몸에서 어떤 역할을 하는지 쉬운 문장 1~2개
3. **대표 사용 목적**: 환자가 이해하기 쉬운 대표 목적 2~3개
4. **더 자세한 허가 목적**: 필요한 사용자만 펼쳐 보는 전체 공식 정보
5. **꼭 기억할 주의사항**과 **바로 의료진에게 알려야 할 증상**
6. **내가 처방받은 복용법**과 **제품 공식 용법**을 별도 영역으로 표시

### 1. 공식 효능 원문 파서 교체

- [x] 문서 제목, 번호, `다음 질환에도 사용할 수 있다` 같은 상투 문구 제거
- [x] HTML/XML 흔적과 불필요한 괄호·공백 정리
- [x] 원문을 질환·증상·목적 단위로 분리하되 문장을 임의 글자 수로 자르지 않음
- [x] 같은 의미의 요약·목적을 정규화해 한 번만 표시
- [x] 일반적인 증상과 전문 질환명을 구분
- [x] 대표 목적은 최대 3개만 선택
- [x] 나머지 허가 목적은 삭제하지 않고 `더 자세히 보기` 영역에 보존
- [x] 원문을 안전하게 나누기 어려우면 긴 목록 대신 공식 정보 펼쳐보기만 제공

### 2. 주성분 역할 설명 구조 개선

- [x] `제품 효능`을 곧바로 `성분 작용 원리`로 바꾸지 않도록 분리
- [x] 검토된 성분 설명 DB를 가장 먼저 사용
- [x] 같은 성분의 다른 제품은 검토 설명을 함께 재사용
- [x] 복합제는 성분별 역할을 각각 저장하고 표시
- [x] 확인된 성분 역할이 없으면 작용을 추측하지 않고 `성분 역할 설명 준비 중`으로 표시
- [x] 공식 출처 기반 설명과 검토된 쉬운 설명을 화면에서 구분
- [x] 향후 e약은요 등 공식 성분 설명 API를 연결할 수 있도록 제공자 인터페이스 마련

### 3. 대표 목적 생성 규칙

- [x] `해열`, `진통`, `소염`처럼 사용자가 이해하기 쉬운 목적 사전 구성
- [x] 약의 공식 허가 범위 안에서만 대표 목적 선택
- [x] 제품별 예외 하드코딩이 아니라 효능 문구와 검토 사전을 이용한 공통 규칙 적용
- [x] `실제 처방 이유는 의료진에게 확인해 주세요` 안내 유지
- [x] 대표 목적과 상세 허가 목적이 같은 문장으로 반복되지 않도록 처리
- [x] 전문 질환명은 대표 영역보다 상세 펼침 영역에 우선 배치

### 4. 품질 판정 단계 추가

- [x] 중복 문장 여부 검사
- [x] 상투적인 문서 제목만 남았는지 검사
- [x] 문장 잘림과 불완전한 끝맺음 검사
- [x] 한 화면에 노출되는 목적 개수와 글자 수 제한
- [x] 성분명만 바꾼 공통 틀 문장을 상세 설명 완료로 판정하지 않음
- [x] 품질 기준을 통과하지 못하면 `READY`가 아니라 `NEEDS_REVIEW`로 분류
- [x] `READY`, `OFFICIAL_ONLY`, `NEEDS_REVIEW`, `PENDING`, `FAILED`, `OUTDATED` 상태를 API와 화면에서 동일하게 처리

### 5. 기존 DB 및 OCR 약 재처리

- [x] 기존 `READY` 검토 설명은 유지
- [x] 현재 생성된 `OFFICIAL_ONLY` 상세 프로필은 새 파서 버전 대상으로 전환
- [x] 앱 DB 전체 약을 새 품질 규칙으로 다시 처리
- [x] 사용자에게 현재 등록된 약을 우선 처리
- [x] 이후 OCR로 공식 코드가 확정되는 모든 약에 새 파서 자동 적용
- [x] 파서 버전을 저장해 이후 규칙 변경 시 필요한 약만 재처리
- [x] 새 처리 결과가 실패하면 기존 안전 정보와 복용 기록은 유지

### 6. Flutter 상세 화면 수정

- [x] `주성분 설명`에는 확인된 역할 설명만 표시
- [x] 공식 정보만 있는 경우 검토된 성분 설명처럼 보이지 않도록 상태 안내 표시
- [x] 대표 사용 목적은 최대 3개까지만 표시
- [x] 전체 허가 목적은 접힌 영역에서 확인하도록 변경
- [x] 목적이 하나뿐이면 요약과 목록을 동시에 표시하지 않음
- [x] 어려운 질환 목록이 첫 화면을 밀어내지 않도록 높이 제한 제거 대신 접기 사용
- [x] 검토 상태와 공식 출처는 화면 아래에서 짧게 표시

### 6-1. 코다론정 기준 `주성분 설명` 1차 시도 — 범위 오류 확인

> 화면 형식과 준비 중 문구는 정리했지만, 부루펜정·계루삼정 예외 처리와 허가 목적 조각 연결에 의존해 모든 약의 설명 품질이 고르게 개선되지 않았다. 레시프람정에서 `공식 허가정보에 따라 주요우울장애`처럼 문장이 끊기는 문제가 확인되어 아래 공통 재수정 계획으로 교체한다.

- [x] `확인된 설명을 준비 중이에요` 문구를 모든 약 상세 화면에서 제거
- [x] `주성분 설명` 카드는 없애지 않고 코다론정과 같은 위치·형식으로 유지
- [x] 단일 성분은 `성분명 + 몸에서 하는 역할 + 치료에 도움이 되는 방식`을 자연스러운 1~2문장으로 표시
- [x] 복합제는 `여러 성분이 함께 들어 있는 약`이라는 짧은 안내 뒤에 성분별 역할을 구분해 표시
- [x] 상단에서 이미 보여 준 성분명 전체를 설명 문장 안에서 그대로 반복하지 않음
- [x] 제품 효능을 성분의 작용 원리처럼 임의로 바꾸지 않고, 검토된 성분 설명 데이터만 사용
- [x] `ingredient_explanations`를 제품별이 아니라 정규화한 성분별 공통 설명 사전으로 확장
- [x] 설명 출처 우선순위를 `검토 완료 DB → 공식 성분 정보 제공자 → 검토 대기 초안`으로 적용
- [x] 검토 대기 초안은 사용자에게 노출하지 않고, 표시 가능한 설명이 확보된 성분부터 카드에 반영
- [x] 부루펜정은 이부프로펜의 검토된 역할 설명을 표시하고 `준비 중` 문구를 사용하지 않음
- [x] 계루삼정은 네 성분명을 한 문장으로 반복하지 않고 공통 역할을 읽기 쉽게 표시
- [x] 코다론정의 현재 `아미오다론염산염은 … 조절하는 성분이에요` 형식을 기준 예시로 유지
- [x] 일반 모드와 쉬운 모드 모두 같은 성분 사실을 사용하고 제목과 안내 용어의 난이도를 구분

### 6-2. 모든 약의 `주성분 설명` 전용 재수정 계획

> 이번 작업의 핵심은 몇 개 제품의 문장을 직접 고치는 것이 아니라, 모든 제품이 공유하는 `주성분 설명` 조회·생성 경로 하나를 고치는 것이다. 허가 목적과 주의사항 등 이미 분리된 영역은 그대로 두고, 레시프람정처럼 주성분 설명이 허가 목적의 일부에서 끊기는 문제만 공통 해결한다.

#### 수정 범위

- [x] 이번 수정은 `주성분 설명` 생성·저장·표시 부분만 변경
- [x] 약 이름, 홈 한 줄 설명, 허가된 사용 목적, 주의사항, 복용법, DUR 화면은 변경하지 않음
- [x] 현재 DB의 모든 약과 이후 OCR로 들어오는 모든 공식 약에 같은 처리 적용
- [x] 특정 제품의 화면 문구를 직접 수정하지 않고 정규화된 주성분을 기준으로 공통 처리

#### 원인 제거

- [x] `approved_uses` 첫 항목을 주성분 설명 뒤에 그대로 붙이는 현재 방식 제거
- [x] `주요우울장애`, `위궤양`, `고혈압` 같은 명사 조각을 완성 문장으로 취급하지 않음
- [x] 원문을 글자 수로 잘라 주성분 설명에 붙이는 처리 제거
- [x] 부루펜정 전용 내장 문구와 계루삼정 전용 성분 묶음 조건 제거
- [x] 제품 코드·제품명을 기준으로 한 약별 예외 분기 금지

#### 공통 성분 설명 데이터 구조

- [x] 정규화된 주성분을 기준으로 설명을 한 번만 저장하고 같은 성분 제품에서 재사용
- [x] 단일 성분 설명 필드를 `역할 설명`, `사용 도움`, `출처`, `검토 상태`, `버전`으로 분리
- [x] 복합제는 각 성분 설명을 개별 조회한 뒤 제품 카드에서 읽기 좋은 순서로 조합
- [x] 공식 성분 설명 제공자를 추가할 수 있는 기존 인터페이스를 실제 조회 경로에 연결
- [x] 출처가 없는 작용 원리나 생성 초안은 사용자 응답에서 제외
- [x] 조회 우선순위를 `검토 완료 성분 설명 → 공식 출처 기반 성분 설명 → 표시 보류`로 고정

#### 완성 문장 생성 규칙

- [x] 검토된 성분 설명이 있으면 코다론정과 같은 자연스러운 1~2문장으로 표시
- [x] 검토된 작용 설명이 없으면 공식 허가 목적을 주성분의 작용 원리처럼 바꾸어 쓰지 않음
- [x] 공식 출처로 완결된 설명을 만들 수 없는 경우에는 부정확한 자동 문장을 응답하지 않고 해당 카드 표시를 보류
- [x] 모든 문장이 완결된 종결 표현으로 끝나는지 공통 검사
- [x] 콜론, 쉼표, 괄호, 접속사 또는 명사만 남은 문장은 표시하지 않음
- [x] 최대 2문장·문장당 최대 길이 기준을 적용하되 글자 중간을 잘라 말줄임표로 만들지 않음
- [x] 상단에는 공식 성분명을 목록으로 보여주고, 설명 카드는 코다론정처럼 `성분명 + 역할`의 완결 문장으로 통일

#### 목표 표시 예시

- [x] 레시프람정: `에스시탈로프람옥살산염은 뇌에서 세로토닌의 작용을 조절해 우울감과 불안 증상을 완화하는 데 도움을 주는 성분이에요.`처럼 한 문장이 끝까지 표시되도록 구성
- [x] `주요우울장애`는 주성분 설명 뒤에 붙이지 않고 기존 `허가된 사용 목적` 카드에서만 표시
- [x] 코다론정도 같은 공통 경로에서 현재처럼 완결된 설명을 유지하며, 별도 제품 예외 코드를 두지 않음

#### 화면 표시 규칙

- [x] `주성분 설명` 카드의 위치와 디자인은 코다론정 화면으로 통일
- [x] 단일 성분은 설명 문단 하나로 표시
- [x] 복합제는 공통 설명 한 문장과 필요한 경우에만 성분별 역할 목록 표시
- [x] `준비 중`, `확인 중`, 잘린 문장, 원문 목록은 주성분 설명 카드에 표시하지 않음
- [x] 설명을 만들 수 없는 예외 상황에서는 불완전한 기본 안내를 만들지 않고 카드 자체를 숨기며, 나머지 상세 정보는 정상 표시

#### 전체 약 재처리

- [x] 설명 파서 버전을 올려 기존 상세 프로필이 새 주성분 설명 규칙으로 갱신되도록 연결
- [x] 현재 DB의 고유 주성분 전체를 공통 생성 경로로 재처리하도록 서버 초기화 경로에 연결
- [x] 코다론정을 포함한 기존 `검토 완료` 성분 설명은 모두 보존
- [x] 잘린 문장·허가 목적 조각 결합·예전 자동 초안으로 판정된 설명만 선별 무효화
- [x] 모든 기존 제품의 주성분 설명이 완결 문장인지 코드 단계에서 검사
- [x] OCR 공식 약 등록 직후 동일한 성분 설명 조회·생성 경로 실행
- [x] 이후 동일 성분 약은 저장된 설명을 즉시 재사용

#### 확인 방식

- [x] 별도의 자체·기기 테스트는 추가 실행하지 않고 사용자 확인 목록만 제공
- [ ] 사용자 확인 대상은 코다론정, 레시프람정, 단일 성분 임의 약 1개, 복합제 임의 약 1개, OCR 신규 약 1개로 정리
- [ ] 레시프람정에서 `공식 허가정보에 따라 주요우울장애`처럼 끊긴 문장이 나오지 않는지 확인
- [ ] 레시프람정의 `주요우울장애`가 `허가된 사용 목적` 카드에는 그대로 남는지 확인
- [ ] 특정 예시 약만 고쳐지고 다른 약이 이전 형식으로 남는지 사용자가 임의 약으로 확인

### 7. 이번 수정의 자체 테스트 — 1개만 계획

- [x] **상세 품질 회귀 자동 테스트 1개**: 코다론정 `READY`, 부루펜정 같은 긴 허가 목록 약, OCR 신규 약을 한 테스트에서 처리해 중복 제거·대표 목적 3개 제한·전체 목적 보존·상태 분류를 확인한다. 실제 기기와 화면 테스트는 사용자가 진행한다. (`1 passed`)

### 사용자 확인 기준

- [ ] 부루펜정에서 `다음 질환에도 사용할 수 있다`가 반복되지 않음
- [ ] 부루펜정 첫 화면에는 해열·통증·염증 등 대표 목적만 간단히 표시됨
- [ ] 전체 전문 질환명은 펼쳤을 때만 표시됨
- [ ] 검토되지 않은 성분 작용을 사실처럼 설명하지 않음
- [ ] 코다론정의 기존 검토 설명 품질은 유지됨
- [ ] 다른 기존 DB 약에서도 긴 공식 원문이 그대로 나열되지 않음
- [ ] OCR로 새로 등록한 약도 동일한 대표 목적·상세 목적 구조를 사용함

---

## 2026-09-13 — 사용자 직접 실테스트 목록

아래 항목은 코드 작업 완료 표시가 아니라 사용자가 직접 확인할 목록이므로, 사용자가 결과를 알려주기 전까지 `[ ]`로 둔다.

- [ ] 서버 재시작 후 앱이 정상적으로 목록을 불러오는지 확인
- [ ] 코다론정 상세가 성분의 역할까지 포함해 자세히 나오는지 확인
- [ ] 코다론정이 아닌 기존 DB 약도 같은 형식의 상세 화면이 나오는지 확인
- [ ] 검토 설명이 있는 약과 없는 약의 안내가 구분되는지 확인
- [ ] 새로운 처방전을 OCR해 공식 코드로 약을 등록
- [ ] OCR 등록 직후 제품명·주성분·복용정보가 먼저 보이는지 확인
- [ ] 상세 준비가 끝난 뒤 같은 약의 자세한 설명이 표시되는지 확인
- [ ] 상세 준비 실패 시 약 등록과 기본 화면은 계속 사용 가능한지 확인
- [ ] 같은 성분의 다른 제품이 공통 성분 설명을 재사용하는지 확인
- [ ] 복합제에서 여러 주성분과 각 역할이 모두 표시되는지 확인
- [ ] Gemini 신뢰도 퍼센트나 검토되지 않은 초안이 화면에 노출되지 않는지 확인
- [ ] 실제 처방 복용법과 제품 공식 용법이 섞이지 않는지 확인
- [ ] DUR 함께먹기 결과가 기존처럼 정상적으로 표시되는지 확인
- [ ] 아디팜정과 비교 약의 상호작용 안내가 상세 구조 변경 후에도 유지되는지 확인
- [ ] 일반 모드와 쉬운 모드 모두 약 상세 화면이 열리는지 확인

---

## 날짜 미정 — 배포 및 후속 작업

### 배포 서버·DB

- [ ] 개발 빌드와 배포 빌드의 서버 주소를 명확히 분리
- [ ] 서버 버전과 DB 출처를 로그 또는 상태 API에서 확인 가능하게 추가
- [ ] 최신 목록·상세·OCR·DUR API를 배포 서버에 반영
- [ ] 배포 DB 백업
- [ ] `MVP-ASP`, `MVP-AMLO`, `MVP-MET` 체험약 비활성화
- [ ] 서버 재시작 후 체험약이 다시 생성되지 않는지 확인
- [ ] OCR 실제 약과 체험약의 출처를 DB에서 구분
- [ ] 기존 실제 처방·복약 기록 유지 여부 확인
- [ ] 배포 서버의 목록·상세 API 404 여부 확인

### 후속 기능

- [ ] Gemini는 부족한 쉬운말 설명의 백그라운드 초안 생성에만 사용
- [ ] 검토가 필요한 AI 설명은 `DRAFT`로 저장하고 공식 기본 정보만 사용자에게 제공
- [ ] 공식 코드가 없거나 매칭이 불확실한 약의 상세 준비 실패 사유 관리
- [ ] 외부 설명 제공자를 교체할 수 있는 인터페이스 분리
- [ ] e약은요 API를 챗봇 또는 상세 설명 보조 출처로 연결
- [ ] 실제 로그인 사용자 기준 전체 흐름 확인
- [ ] 보호자 기능은 현재 환자 범위에서 제외하고 추후 별도 검토

---

## 날짜별 진행 로그

| 날짜 | 구분 | 내용 |
|---|---|---|
| 2026-09-11 | 완료 | 내 약 목록 API와 화면 연결 |
| 2026-09-11 | 완료 | 홈·목록·상세의 약 이름, 주성분, 쉬운 설명 표시 통일 |
| 2026-09-11 | 완료 | OCR 신뢰도·복용정보 저장·등록 흐름 보강 |
| 2026-09-11 | 완료 | 상세 화면 오류 수정과 DUR 비교 데이터 준비 |
| 2026-09-11 | 완료 | 코다론정 기준 상세 설명 1차 구조 적용 |
| 2026-09-13 | 계획 | 전체 DB 약과 OCR 신규 약에 적용할 공통 상세 설명 구조 추가 |
| 2026-09-13 | 완료 | 공통 상세 저장 구조·전체 DB 준비·OCR 자동 연결·상태별 상세 화면 코드 적용 |
| 2026-09-13 | 확인 | 허용된 통합 자동 테스트 1개 통과 |
| 2026-09-13 | 계획 | 미검토 약의 원문 나열·중복·전문용어 과다 노출을 해결하는 상세 설명 품질 2차 수정 |
| 2026-09-13 | 완료 | 대표 목적 3개·전체 허가 목적 접기·성분 설명 검토 구분·`NEEDS_REVIEW` 품질 판정 적용 |
| 2026-09-13 | 확인 | 상세 품질 회귀 자동 테스트 1개 통과 |
| 2026-09-13 | 계획 | 검토 설명이 없는 약에서 `확인된 설명을 준비 중이에요` 카드와 복합 성분 반복 표시 제거 |
| 2026-09-13 | 완료 | 모든 DB·OCR 약의 주성분 설명 카드를 공통 생성 경로로 통일하고 준비 중 문구 제거 |
| 날짜 미정 | 대기 | 배포 DB 정리와 배포 서버 반영 |
